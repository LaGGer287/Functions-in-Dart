import 'dart:io';

void inChan() {
  print("Enter the number: ");
  int n = int.parse(stdin.readLineSync()!);
  print("Even numbers in the input range is: ");
  for (int i = 0; i <= n; i++) {
    if (i % 2 == 0) {
      print(i);
    }
  }
}

void main() {
  inChan();
}